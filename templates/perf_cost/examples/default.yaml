# Default preset for performance/cost analysis
repo_path: "."
scope:
  - "."
time_budget_minutes: 60
depth: normal
output_format: markdown
risk_tolerance: balanced
audience: senior_eng
focus_areas:
  - cpu
  - memory
  - io
  - network
